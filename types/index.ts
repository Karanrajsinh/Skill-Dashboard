export interface ChartData {
  percentile: number;
  students: number;
}

export interface SyllabusData {
  title: string;
  progress: number;
  color: string;
}

export interface ScoreData {
  rank: number;
  percentile: number;
  score: number;
}